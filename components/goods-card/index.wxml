<view
  id="{{independentID}}"
  class="goods-card rural-section-card"
  bind:tap="clickHandle"
  data-goods="{{ goods }}"
>
  <view class="goods-card__main">
    <view class="goods-card__thumb" bind:tap="clickThumbHandle">
      <t-image
        t-class="goods-card__img"
        src="{{ thumbSrc }}"
        mode="aspectFill"
        lazy-load
        bind:error="onThumbError"
      />
      <view wx:if="{{goods.ruralSupport}}" class="rural-support-badge">助农</view>
    </view>
    <view class="goods-card__body">
      <view class="goods-card__upper">
        <view wx:if="{{ goods.title }}" class="goods-card__title rural-text-title">
          {{ goods.title }}
        </view>
        <view wx:if="{{ goods.tags && !!goods.tags.length }}" class="goods-card__tags">
          <view
            wx:for="{{ goods.tags }}"
            wx:key="index"
            wx:for-item="tag"
            class="goods-card__tag rural-tag"
            data-index="{{index}}"
          >
            {{tag}}
          </view>
        </view>
      </view>
      <view class="goods-card__down">
        <price
          wx:if="{{ goods.price }}"
          wr-class="spec-for-price rural-price"
          symbol-class="spec-for-symbol"
          symbol="{{currency}}"
          price="{{goods.price}}"
        />
        <price
          wx:if="{{ goods.originPrice && isValidityLinePrice }}"
          wr-class="goods-card__origin-price rural-old-price"
          symbol="{{currency}}"
          price="{{goods.originPrice}}"
          type="delthrough"
        />
        <t-icon
          class="goods-card__add-cart"
          id="{{independentID}}-cart"
          data-id="{{independentID}}"
          catchtap="addCartHandle"
          name="cart-add"
          size="24rpx"
          color="#333"
        />
      </view>
    </view>
  </view>
</view>

